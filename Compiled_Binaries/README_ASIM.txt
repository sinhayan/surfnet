Linux_x86_64 is the compiled binary.

BlendedIntrinsicMaps is the script which launches this binary, giving this binary the various parameters and settings and file location it needs to run on.

Other files are some settings files and other types of Non-Compiled files which are used by the executable during run time.
